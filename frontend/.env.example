# Non necessaria di norma: in dev, vite.config.ts proxya /api verso
# http://localhost:8000 automaticamente; in produzione nginx fa lo stesso
# (vedi frontend/nginx.conf). Impostala solo se vuoi puntare il frontend
# locale a un backend diverso da quello di default.
# VITE_API_BASE_URL=http://localhost:8000
